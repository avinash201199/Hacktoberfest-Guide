name: "Issue Greeter"
on: [issues]

jobs:
  issue-greeter:
    runs-on: ubuntu-latest
    steps:
      - name: "Greeter"
        uses: avinash201199/greeter@V1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          message: Welcome to the **Greeter** repo on **Github**.
